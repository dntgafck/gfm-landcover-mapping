lr: 1e-3
weight_decay: 1e-4
ignore_index: 255
loss:
  name: "weighted_ce"
  dice_weight: 0.2
  class_weights_path: "data/stats/class_weights.json"
  compute_if_missing: true
  min_weight: 0.25
  max_weight: 4.0
scheduler:
  name: "cosine"
  step_size: 20
  gamma: 0.1
  T_max: 50
