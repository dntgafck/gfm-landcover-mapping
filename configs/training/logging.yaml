# MLflow configuration (optional mirroring)
mlflow:
  enabled: false
  tracking_uri: "http://127.0.0.1:8080"
  experiment_name: "landcover_segmentation"
  run_name: null
  # Mirror artifacts from run_dir to MLflow
  mirror_config: true
  mirror_plots: true
  mirror_checkpoint: false # Can be large, disabled by default
