# Export configuration for ONNX models
# TensorRT inference is handled by ONNX Runtime's TensorrtExecutionProvider

# Base export settings
enabled: true
onnx_filename: "model.onnx"
checkpoint: "best" # "best" or "last"
opset_version: null # ONNX opset version (null = auto-select, e.g., 17, 18)
